interface:
  display_name: "Volcengine Web Search"
  short_description: "Search the web via Volcengine"
  default_prompt: "Use $volcengine-search-web to search the user's question. If VOLCENGINE keys are missing, report the gap."
policy:
  allow_implicit_invocation: true
