---
description: 允许抄触动（API/逻辑/算法）；全项目CPU越低越好；内存释放对照触动
alwaysApply: true
---

# 子砚 · 业务 API / CPU / 内存铁律（2026-08-02 修订：可抄触动）

凡本仓库开发、联调、验收、改找色/触控/Toast/截帧/守护，**必须**遵守。

---

## 1. 允许抄触动精灵（写死）

为追平/超越触动，**允许直接抄、照搬、移植**触动侧可观察与公开资料中的实现思路与接口形态，写入 ZiYan 自有树。

| 允许抄 | 说明 |
|--------|------|
| **业务 Lua API** | 函数名、参数顺序、默认值、返回值、错误语义、脚本写法（如 `findMultiColorInRegionFuzzy` / `toast` / `tap` / `keepScreen` / `init` / `mSleep` / `touchDown|Up` / `pressHomeKey` 等） |
| **逻辑与算法** | 找色、合帧、keep、Home 后可点、常驻缓冲、节流、启停模型等；可按手册 / `.171` 日志 / 二进制字符串 / `open_code` / corpus **对照移植**进 `lua/` `objc/` |
| **架构模型** | Daemon/单宿主、热帧常驻、IPC 分层、音量启停体感等 |

| 仍禁止（仅此两条） | 说明 |
|--------------------|------|
| **运行时链依赖** | 禁止把触动 `.dylib` / `TSDaemon` / 私有 so **链进 ZiYan 包或当作本机运行依赖**（实现须落 ZiYan 自己的二进制/脚本） |
| **观察机当验收** | `.149`/`.171` 只读对照；PASS 只出在 `.53/.101/.112/.166` |

> **旧表述全部作废**：  
> 「仅作原理学习、严禁复用/导入/调用触动模块接口源码」「禁止出现触动原生函数名」「必须重新自主封装、禁止照搬」——**一律作废**。  
> 以本条为准：**可以抄触动；代码落 ZiYan 树、不链触动二进制运行时。**

桌面测脚本：`ios7.lua` / `ios8p.lua` 仍仅 scp，不改内容（与是否抄触动无关）。

---

## 1b. 自测前清机（铁律）

每次在 `.53/.101/.112/.166` 自测前必须：

1. 停脚本意图、杀业务 lua/游戏/ZiYan App  
2. 收割僵尸（Z）、保证单一 `ziyan_framecap serve`  
3. 释帧 / Home / 清 force 残留  

命令：`bash tools/zy_pretest_clean_4phone.sh`。**不清不测**，以免污染新门禁。

---

## 2. CPU：全项目占用越低越好

优化与评审默认以 **更低 CPU** 为硬目标（对标并争取低于同脚本下的触动稳态）：

1. 禁无意义热轮询（framecap / color_req / toast / force_recap 风暴）。
2. 同文案 toast、空闲合帧、桌面中继必须节流；忙时轮询也优先抬间隔而非 1ms 空转。
3. 默认勿 auto-keep；锁帧须脚本显式 `keepScreen(true)` 或明确 opt-in。
4. 改动若可能抬高 SB / backboardd / framecap CPU，必须附真机前后对比或说明回滚点。
5. 验收关注：脚本长跑时 SB/backboardd/framecap 是否可持续、无重启环（见五机 VERDICT 门禁）。

---

## 3. 内存释放：对照触动精灵

1. 缓冲/截帧/Toast/OCR：**用完即释**，禁止无界 append、禁止死循环叠 UIWindow/大图。
2. keep/shm/像素堆：语义对齐触动——解锁帧即清或移交；禁止脚本停后仍 `keepScreen=1` 锁旧帧。
3. 长跑对照 `.171`：同业务脚本下观察触动是否崩、RSS 是否飘；子砚不得明显更差。
4. 优先 `malloc_zone_pressure_relief` / 截断日志 / 复用单一 toast 窗，禁止每圈新建重资源。
5. 真机若出现 SB RSS 短时暴涨、jetsam、backboardd 连环崩：先停脚本，再按触动侧行为收紧释放路径。

---

## 4. 工作流（与四支柱衔接）

```
.171/.149 拉 TS 日志 / 手册 / open_code / 逆向线索
  → 允许抄 API + 逻辑 + 算法进 lua/objc（不链 TS 二进制）
  → 实现降 CPU、对照触动做释放
  → 仅 .53/.101/.112/.166 真测
  → VERDICT：CPU / 崩溃 / 圈速 / 命中
```

权威索引：本规则 · `ROADMAP.md`（Z0–Z3 预算与刀序，唯一真值源）· `surpass-ts-four-pillar.mdc` · `DEVICE_RULES.md`
