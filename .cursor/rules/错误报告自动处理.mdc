---
description: ZiYan 错误报告自动导入与单主题修复
alwaysApply: true
---

# 错误报告自动处理规则

## 1. 触发口令

当用户表达以下任一意思时，自动执行本规则：

```text
连接 SSH 或 USB，提取并安全分析 ZiYan 错误报告
连接 ssh 或 usb 提取错误报告
导入手机错误报告并安全分析
处理问题报告 <问题编号>
```

用户不需要重复说明手机路径、项目路径、索引路径或 AI 读取顺序。默认是安全诊断模式：允许导入和分析，不允许改源码、构建、部署或写入真机。

只有收到以下精确授权后才提升权限：

```text
授权修复问题报告：<问题编号>。只允许修复：<问题主题>。不得修改其他模块，不得部署到手机。
允许部署并真机验证问题报告：<问题编号>。
```

## 2. 必读文件

执行任何导入、分析或修复前，按顺序读取：

1. `错误报告处理说明.txt`
2. `ROADMAP.md`
3. `今日项目进度.txt`
4. `.cursorrules`
5. `.cursor/skills/ziyan-verdict-loop/SKILL.md`
6. `DOCS/智能体问题索引.md`

## 3. 自动导入

手机端逻辑源路径：

```text
/var/mobile/Media/ZiYan/问题报告/待处理/
```

rootful 实际源路径：

```text
/private/var/mobile/Media/ZiYan/问题报告/待处理/
```

rootless 实际源路径：

```text
/var/jb/var/mobile/Media/ZiYan/问题报告/待处理/
```

项目端固定接收路径：

```text
/Users/mac/Desktop/ZiYan_副本/tmp_shots/智能体问题报告/待处理/<问题编号>/
```

处理规则：

1. 优先使用已可用 SSH；USB 仅在 SSH 不可用且现有 USB 通道可用时使用；
2. 用户指定设备时只处理该设备；未指定时只检查 `.53/.101/.112/.166`，不向 `.149/.171` 部署或写入 ZiYan；
3. 自动识别 rootful/rootless 后读取对应实际源路径；
4. 只导入完整报告目录：必须有 `issue.json`、`summary.txt`、`evidence_manifest.json`；
5. 原样复制整个问题目录，保留文件名、时间和 hash；
6. 目标目录已存在且 hash 相同则去重，不覆盖；hash 不同则保留为新版本并记录冲突；
7. 不能连通、路径不存在或报告不完整时，写清 `TRANSPORT_BLOCKED` 或 `INCOMPLETE_REPORT`，不改产品代码；
8. 不把原始报告复制到 `lua/`、`objc/`、`layout/`、项目根目录；
9. 不读取/导入密码、验证码、手机号、联系方式、私聊原文或敏感截图。

## 4. 索引与问题选择

导入成功后，在 `DOCS/智能体问题索引.md` 新增或更新一行。

问题指纹由“函数/规则 + 错误码 + 环境 + 状态/堆栈摘要”组成。同一指纹只更新出现次数和最新证据，不重复创建无数问题。

若一次导入多条完整报告：

1. 全部登记为 `NEW`；
2. 优先处理 P0，其次 P1，再按最近时间；
3. 单次只修复一个问题主题；其余保持 `NEW`，不得混改。

## 5. 自动分析与修复边界

对选中的 `<问题编号>`，按以下顺序读取：

```text
issue.json
→ summary.txt
→ evidence_manifest.json
→ events.jsonl 的相关时间窗口
→ issue 指向的函数/规则源码
→ 最近相关 VERDICT 与现有证据
```

默认仅执行：

```text
完整性检查
→ 最小复现分析
→ 归属层分类
→ 修复计划与回滚点
→ 更新索引
```

收到“授权修复问题报告”后，才允许执行一个最小修复与离线测试；收到“允许部署并真机验证问题报告”后，才允许在 `.53/.101/.112/.166` 执行会改变设备状态的测试。

只允许修复报告明确指向的单一主题：函数缺失、Lua/Python 错误、CPU/RSS、泄漏、视觉、触控、前台、生命周期、网络或规则问题。

严禁：

```text
修改无关功能
修改用户手写脚本或 Desktop ios7.lua / ios8p.lua
删除原始报告、tmp_shots、VERDICT、回滚包或历史证据
杀/重启 SpringBoard 或 backboardd 掩盖问题
清系统网络缓存、反复重启 Wi-Fi、修改路由器配置
新增竞争守护或提高热轮询频率
因为报告不完整而猜测性改代码
```

## 6. 完成输出

自动处理结束必须输出：

```text
导入设备与问题编号
项目证据路径
问题归因
改动文件
最小测试和真机证据
CPU/RSS、FC_N、keep、前台、停止清理结果
最新 VERDICT 路径
回滚点
索引状态
下一条唯一动作
```

未验证前不得宣称问题已修复、完全兼容或全面超越。
