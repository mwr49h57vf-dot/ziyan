# 本轮修复状态

2026 年 9 月 12 日。基线 cfb4695bd83c8a11dbaf428b1bc0c9f58d39b090。

19 项修复的代码和本地回归已落到工作区。完整方案尚未完成最终验收。
用户确认暂时没有 Mac 构建机。当前缺 Theos、iOS SDK 和本轮候选安装包，五机验收未执行。
用户已授权推送到 develop。本轮按模块提交源码、测试与证据；不发布安装包或关闭 Issue。源码及测试哈希见 candidate-source-sha256.json。

## 逐项状态

1. #1 打包边界：暂存变量收敛到单一进程，每次写入检查目录边界并传播失败。替身与真实 Make 退出码验证通过。双包构建待验证。
2. #2 数据迁移：复制和比对完成后保留来源备份。同名不同内容保留双方。故障、中断、重试和并发冲突测试通过。真实设备数据副本升级待验证。
3. #3 热更新状态：检查安装退出码、完整 dpkg 状态、版本、架构及回滚。提交版本前必须通过实际运行探测回调。未配置回调时拒绝安装。Lua 故障测试通过，真实 dpkg 和设备运行检查待验证。
4. #4 管理发布：独立管理凭证、导入目录限制和真实 deb 元数据校验。HTTP 与浏览器授权、发布验收通过。未发布真实包。
5. #5 截图配对：默认回环，本机显式开启短期配对，凭证关联客户端 IP。客户端 HTTP/Tk 和 C 授权契约通过。设备设置 UI 尚未接入，当前启用入口是本机命令。原生监听与五机待验证。
6. #6 脚本导入：协调读取、同目录临时副本、完整内容比对、最后原子替换。失败保留旧文件，界面逐项显示结果。已完成静态复核并提供 Foundation 行为测试；当前缺 SDK，原生测试和文档选择器尚未运行。
7. #7 内存缓存：旧格式统一使用原生 plist helper，转换失败不返回其他应用结果，也不能写出遮蔽旧状态的新缓存。读取、列键、扫描、导出、写入传播错误。Lua 回归通过，原生 helper 及双方案设备待验证。
8. #8 离线队列：打开、写入、关闭、rename 和状态提交失败向上报告，未可靠接管的原报告受保护。故障注入通过。六个独立 Lua 进程完成真实磁盘和 HTTP 断开、恢复、重启验证。未模拟断电或文件系统损坏。
9. #9 日志并发：正文与索引通过事务记录恢复，同编号冲突返回 409。并发重投及真实进程中断恢复通过。
10. #10 版本判断：服务端使用 Debian 版本语义，设备端调用 dpkg 版本比较，安装准备后再读取已装版本。同版本与降级默认拒绝。本地验证通过。
11. #11 慢客户端：完整发送采用单调时钟期限，错误、超时和异常清理连接及忙碌标记。生产 C 层与真实 socket 验证通过，设备 8 秒期限待验证。
12. #12 请求完整性：收齐受限请求头和正文后执行业务。分段与整包 5KB 请求等价，截断、超时、超限和错误编码拒绝。本地 C/socket 通过，原生找色业务待验证。
13. #13 AI 判定：执行成功与业务成功分开。false 失败，nil 和无入口未验证，旧 main 不可被复用。矩阵透传能力证据要求。真实 Lua 回归与既有合同通过。
14. #14 APT 校验：要求包身份、数量、双架构、索引和 Release 哈希，并使用明确可信公钥环实际验签。真实 gpgv 正反例通过。真实候选仓库待验证。
15. #15 Thread.wait：主线程休眠，协程挂起并恢复，兼容入口一致，保留定时任务调度。Lua 5.3.5 测试通过。
16. #16 部署结果：预检、上传、安装或验收失败都会反映到最终非零退出码。认证探测与业务脚本分离，避免空 heredoc 重试吞错。命令替身测试通过，没有执行部署。
17. #17 侧车验收：C 与 Python 词法契约一致，要求阶段函数声明，拦截禁止依赖和无法确定的动态加载。两端均以真实 Lua 只编译语法。正常脚本、坏语法、动态参数及不执行业务测试通过。原生客户端完整交互待验证。
18. #18 多窗口刷新：请求绑定原窗口、设备、方向与编号，过期结果丢弃，实时模式固定窗口。真实 Tk、多窗口、连续刷新和发行源码入口通过。未重新打包 Windows exe。
19. #19 OCR：全屏写法共用尺寸限制，小区域放大也受最终像素上限限制，异常区域明确失败。实际 C 几何代码验证通过。UIKit 图像处理、识别结果、内存和耗时待验证。
20. #20 集成验收：本地回归与审查已完成，尚未生成双包，也未进行规定五机验收。保持未完成。

## 验证记录

安装打包组 23 项通过，服务 HTTP 14 项通过，热更新 Lua 32 项行为断言通过。
截图组 18 项 HTTP、Tk、socket 与发行源码入口测试通过，另有 C 传输契约和格式回归。
主任务补充了实际 Lua、实际 C、侧车 HTTP、语法检查与队列磁盘重启测试。命令、输出和跳过项保存在 lua-native-local-results.json。
原生 Foundation 测试因缺 SDK 明确跳过。源码文本检查没有计作原生行为测试通过。

记录分别见 build-install-evidence.md、server-update-evidence.md、snapshot-client-evidence.md、queue-disk-restart-evidence.md 和 lua-native-evidence.md。

## 审查与限制

独立审查发现并修复了矩阵漏传证据参数、离线上报后的状态提交错误、侧车动态依赖绕过、长参数截断、无阶段函数和坏语法误接受。
主任务复核并补齐了旧式 LZMA 包支持、热更新缺运行探测时拒绝安装。逐组源码哈希与测试记录一致，git diff --check 通过。
CodeGraph 未连接，当前 checkout 缺 AI_EXECUTION_PROTOCOL.md。调用方审查限于本轮直接模块，没有改 AgentAutonomousEngine 或受保护业务脚本。

仍存在一项本轮范围外的基线问题：管理页“导出桌面”按钮调用未定义的 buildQuery。当前发布和日志下载验收没有调用该按钮，不能把它算作已验证。
依赖检查只用于脚本生成契约，不构成通用 Lua 沙箱。

## 接续条件

具备 Mac、Theos、iOS SDK 和所需 vendor 后，先完成原生测试及 rootful/rootless 候选构建，固定源码、两种包和资源哈希，再按 .101、.112、.166、.53、.61 顺序验收。
需取得每机当前包的 final verdict、FC_N=1、SB_CHG=0 和停止清理证据。
缺少 .61 的结果时不得提升为主计划 PASS。原主计划未覆盖 API、兼容矩阵和发布条件继续保留。
