# 构建、安装、APT 与部署修复证据

日期：2026 年 9 月 12 日。

对应工单：#1、#2、#14、#16。

源码基线：`cfb4695bd83c8a11dbaf428b1bc0c9f58d39b090`。本轮没有提交、发布、修改 Issue、SSH 或设备写入。所有可变输入均为 `tests` 下的独立临时目录。测试后清理这些临时目录。

## 本地结论

当前 23 个测试通过，退出码 0，用时 34.471 秒。同一组核心回归在基线副本上得到 6 个断言失败，退出码 1，没有测试执行错误。另一个新回归先复现了尾部 `defense.log` 同名冲突仍删除旧数据的问题，修复后通过。

测试命令：

```powershell
$env:PYTHONUTF8='1'
& 'C:/Users/Administrator/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe' tools/test_build_install_repairs.py
```

1. #1：资源暂存集中到 `zy_stage_runtime.py`。验证目的目录必须存在、不能是根目录，且必须严格位于指定构建目录内。每次变更都检查目的路径及已存在的越界符号链接。复制、同步、链接、权限、签名、重写和清理失败会停止该次暂存。使用两个包前缀的命令替身检查路径。另由真实 GNU Make 执行当前 recipe，验证暂存子进程返回 42 时不会执行打包完成目标。保留原有双包 control 恢复与架构检查脚本。

2. #2：迁移先复制到临时文件，再核对内容。使用硬链接发布已核验文件，避免覆盖同时出现的目标。不同内容的同名文件保留双方并返回失败。全部复制成功后，将源目录移入独立备份目录，保存来源和目标清单，设置源备份只读。目录、内部脚本、运行状态迁移和尾部防御资源迁移使用同一规则。覆盖复制失败、模拟磁盘写入失败、权限失败、复制中断、归档后中断、并发同名文件、隐藏文件、嵌套文件与重复安装。默认 USB 示例也不再覆盖现有文件。

3. #14：验证预期包身份、架构、数量、可选版本、索引压缩内容、包文件大小与 SHA256。Release 要求必要字段、明确时区和完整索引覆盖。签名文件存在与签名验证结果分别输出。只有调用者指定的公钥环参与验签。实际调用 GnuPG 2.4.8 的 `gpgv`，同时验证 detached 签名与 clearsign 内容。正常双架构测试仓库通过；错误信任材料、未提供信任材料、伪签名、篡改、不同 InRelease 内容、缺架构、空索引、重复记录、缺 Release 字段、漏哈希、错误大小和压缩内容损坏均失败。

4. #16：逐设备收集预检、上传、安装和验收结果。任何阶段失败都会使最终退出非零，并停止输出 `DEPLOY_DONE`。仍继续收集其他设备的结果。额外修复了 SSH 认证重试吞错：先用无 stdin 的请求选择认证方式，然后业务脚本只执行一次。远端返回 12 会原样返回，不能通过重试一个已耗尽的 heredoc 变成 0。密码认证分支保留完整脚本与非零返回值。全部部署测试使用命令替身，没有连接设备。

## 日志与标识

1. `build-install-baseline.log`：基线 6 项断言失败，退出码 1。
2. `build-install-local.log`：当前 23 项通过，退出码 0。
3. `apt-verification.log`：真实 CLI 和真实 gpgv 验签输出，退出码 0。
4. `apt-fixture-identifiers.json`：一次性测试公钥指纹及测试仓库文件 SHA256。
5. `build-install-checks.json`：相关源码 SHA256、shell 语法检查、Python 语法检查及 `git diff --check` 结果，退出码均为 0。

APT 正向验收使用一次性内存 RSA 密钥。测试辅助文件生成 OpenPGP 签名，独立的真实 gpgv 执行密码学验证。此做法不调用 Windows 的 GPG 建钥代理，也不保存私钥。公钥指纹为 `037A351DFFC74D01972826BFF94B64F88ABBF6DE`，公钥环 SHA256 为 `5e452e26c458c4b552989314d87fdd9a30307ccf4abe7d8a9e1b7cad73593226`。

APT 测试中的 `.deb` 是合成文件，只验证仓库完整性契约。它们不可安装，也不是本轮 ZiYan 候选包。这里没有评价任何历史发布签名。

## 未验证边界与后续验收

Theos 环境变量为空，默认 Theos 目录不存在。本机没有可用 iOS SDK、`xcrun`、`install_name_tool` 或 `ldid`。没有生成 rootful/rootless 候选包，因此没有候选包哈希和解包完整性证据。CodeGraph 工具未连接。项目 preflight 脚本包含 SSH 探测，依本轮隔离约束没有执行。

#1 的双包真实构建与解包、#2 的两种设备升级与回退、#14 的真实候选仓库和发布信任材料、#16 的既有部署通道读回均仍需真实环境验收。设备 `FC_N`、`SB_CHG`、停止清理状态均未测量。未产生任何 DEVICE_PASS 或五机最终结论。

现有部署工具仍包含原有的系统界面重载分支。本轮只测试替身，不代表允许执行该分支。实际部署必须遵守当前任务的独立授权和设备门禁。

回退点为上述源码基线和当前未提交补丁。迁移回退使用 `.migration-backups/committed.*/source` 及对应 `manifest.txt`，不删除用户目录或覆盖冲突目标。独立审查由主代理另行记录。
