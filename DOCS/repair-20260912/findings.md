# 实施发现

原审计 19 项已映射为 #1 至 #19，票验收快照见 tickets.json。
本地 checkpoint 记录其他历史发布工作，不能覆盖本轮独立修复授权。
Windows 默认 GBK 导致 checkpoint 工具读取失败，读取使用 UTF-8；未改历史 checkpoint。
Python 使用 Codex 官方随附运行时，C 编译器为既有 Strawberry GCC，shell 为既有 Git Bash。
CodeGraph 未连接、iOS SDK 缺失、设备通道结果单列，不能作为本地测试通过依据。
