# 工程审计 · 主计划剩余进度

审计时间：2026-09-10T17:18+0800（证据快照）
工作目录：`/Users/mac/Desktop/ZiYan_副本`
审计对象：ZiYan 主计划（`ROADMAP.md` + 迁移/五机验收阶段）剩余进度
依据：`.codex/ZIYAN_ACTIVE_CHECKPOINT.json`、`.codex/CONTINUATION_FIRST_MESSAGE.txt`、
`DOCS/工程审计_迁出任务审核_20260909.md`、`DOCS/工程审计_迁出任务审核_20260910.md`、
`tmp_shots/E48_DEVICE*`、`tmp_shots/API101_DEVICE*`、`tests/touchsprite_migration/`、
`api_spec/device_function_matrix.json`

---

## 0. 结论（一句话）

主计划**未完成**。五机 E48 受控矩阵在“样本层”已实质闭合（2026-09-10 12:15–12:18），
但**当前包一致性、`.61` 专用 rootless contract 工件、22 个来源样本的运行化重写、
101 项功能矩阵、动态库运行加载闭环、核心回归、缺陷清单与发布判定**全部未完成。
当前 checkpoint 的 `unfinished=false` 是**合同测试副作用**，不是真完成。

---

## 1. 已证据确认完成的部分

### 1.1 Phase 0 / 0.4 · 来源只读采集

- `e48_legend_manifest.json`：允许记录 40 个、敏感排除 41 个；来源根 `E:\传奇世界`，`accessMode=read_only_copy`。
- 状态：**完成**（只读采集，不含运行兼容性）。

### 1.2 Phase 0.5 · 迁移副本

- 22 个候选已复制到 `tests/touchsprite_migration/source/`，`sample_mapping.json` 全部 `copyStatus=verified`。
- 但运行化结果（`tests/touchsprite_migration/migration_resolution.json`）：
  - `MIGRATED_EXECUTABLE` **2**（`系统工具/main.lua`、`系统工具/ZYXiTongGongJu.lua`）
  - `MIGRATED_RESOURCE` **1**（`2015528121953696.jpg`）
  - `BLOCKED` **19**（`unresolved_dependencies` / `source_syntax_or_encryption_failure` / `host_shell_or_file_mutation` / `remote_ftp_dependency` / `unbounded_loop_requires_stop_token` 等）
- `api_mapping.json` 仍 `staticOnly=true`。
- 状态：**复制完成，运行化 3/22，未完成**。

### 1.3 E48 五机受控矩阵（样本层）

| 设备 | 结果 | 包 | 包 SHA256 | 证据 |
|---|---|---|---|---|
| `.101` | 9/9 PASS | `…17-147`(arm) | `cba354ba…826a` | `tmp_shots/E48_DEVICE101_20260909_211121/SUMMARY.txt` |
| `.112` | 9/9 PASS | `…17-147`(arm) | `cba354ba…826a` | `tmp_shots/E48_DEVICE112_20260909_204440/SUMMARY.txt` |
| `.166` | 原矩阵 8/9 + `cold_start` replacement PASS | `…17-147`(arm) | `cba354ba…826a` | `tmp_shots/E48_DEVICE166_20260909_213954/` + `…_214618/`(`run_id=e48_cold_start_166_18548_1788961579`) |
| `.53` | 原矩阵 8/9 + `cold_start` replacement PASS | `…17-148`(arm64) | `a82f2a54…d8ab8` | `tmp_shots/E48_DEVICE53_20260910_002642/` + `…_121759/`(`run_id=e48_cold_start_53_21308_1789013880`, `foreground_active_seen=1`, `final_verdict=PASS`) |
| `.61` | 9/9 PASS | `…17-151`(arm64) | `5f62c6ce…1168` | `tmp_shots/E48_DEVICE61_20260910_121258/SUMMARY.txt`（`all_cases_pass=1`） |

- `.61` 在本轮 12:12–12:15 完成 iOS 15.8.8 rootless 九用例矩阵，**已不再 TRANSPORT_BLOCKED**（该阻塞在 06:1x 通过 dpkg `half-configured` 修复解除：`tmp_shots/E48_DEVICE61_INSTALL_REPAIR_CONTRACT_20260910_060542/repair.txt` 显示 `INSTALL_OK no_auto_respring=1`）。
- `.166` / `.53` 的**原始 SUMMARY 仍是 `all_cases_pass=0`**，只能表述为“原矩阵 8/9 + 唯一缺项独立 replacement PASS”，不得压成 9/9。

### 1.4 101 项非 Agent API · 仅绑定探测

- 五机 `tmp_shots/API101_DEVICE{101,112,166,53,61}_20260910_1233/VERDICT.txt`：
  `API_101_BINDINGS total=101 present=86 missing=15`。
- `api_spec/device_function_matrix.json` → `execution_summary`: `device_runs=5, runtime_present_per_device=86, blocked_per_device=15, functional_pass=false`。
- 101/101 个用例 `local_functional_pass=false`；设备覆盖仅为 `RUNTIME_BINDING_ONLY` + `NOT_FUNCTIONALLY_TESTED`。
- 15 个 `MISSING_RUNTIME`：`sys.notifyVibrate`、`sys.notifyVoice`、`screen.findMultiColorInRegionFuzzyEx`、`screen.snapshotRegion`、`image.*`(5)、`ocr.localOcrText`、`ocr.cloudOcrText`、`codec.*`(4)。
- 状态：**绑定探测完成，功能矩阵未开始**。

---

## 2. 主计划剩余项（按依赖顺序）

| # | 剩余项 | 当前状态 | 证据/缺口 |
|---|---|---|---|
| 1 | `.61` 专用 iOS 15.8.8 rootless contract 工件 | **INVALID_RUN**（06:10，`root_password_rc=5`） | `tmp_shots/E48_DEVICE61_ROOTLESS_CONTRACT_FINAL_20260910_061021/SUMMARY.txt`；12:15 的九用例矩阵不能替代该专用 contract 工件 |
| 2 | 五机“当前包”一致性 | **不成立** | E48 用 147/148/151，API 探测用 152/153；此后又建 149/150/152/153/154/155，其中 **154/155 无任何设备证据**。`.101` 现装 152，与其 E48 的 147 已不同 |
| 3 | 迁移 19 个 BLOCKED 样本逐样本重写 + 最小复现 | 未开始 | `tests/touchsprite_migration/migration_resolution.json` |
| 4 | 101 项按能力族真机功能矩阵（normal/error/repeated/timeout/abnormal_exit/stop_cleanup/rootful/rootless） | 未开始 | 现全部 `NOT_FUNCTIONALLY_TESTED` |
| 5 | 动态库运行加载闭环（rootful/rootless 路径、依赖修复、设备加载验证、五机回归） | 仅静态审查 | `DOCS/dylib_static_analysis_2026-09-08/`，仍 `STATIC_REVIEW_ONLY` |
| 6 | 核心回归族（生命周期/触控/图色/OCR/Lua/IPC/文件网络/脚本管理/异常退出/停止/重复运行/资源缺失/断网） | 未开始 | 本轮无当前包证据 |
| 7 | 缺陷清单 + 修复提交 + 五机状态 + 正式发布判定 | 未开始 | `ROADMAP.md` §0 两条硬指标未重测 |

---

## 3. 审核发现（必须纠正的口径/状态问题）

1. **checkpoint 被合同测试污染（缺陷）**：
   `tools/test_checkpoint_finished_state.py` 直接对真实
   `.codex/ZIYAN_ACTIVE_CHECKPOINT.json` 调用 `tools/ziyan_codex_checkpoint.py write`，
   测试结束把真实断点覆盖为
   `stage=checkpoint-contract / lastSuccessfulCommand=test_checkpoint_finished_state.py /
   literalResult=contract test / deviceState=本轮未连接设备 / unfinished=false /
   nextAction=等待人工最终审核`。
   当前 checkpoint 与测试脚本中的常量逐字一致，可确认是**测试残留**，不是主计划完成。
   该测试应写临时文件，`ziyan_codex_checkpoint.py` 也不应把 `WORKTREE_STATE` 硬编码到
   `/Users/mac/.codex/worktrees/edf5/...`。
2. **续接提示词已过期**：`.codex/CONTINUATION_FIRST_MESSAGE.txt` 仍写
   `.61 TRANSPORT_BLOCKED`、`.53 cold_start INVALID_RUN`、`FIVE_DEVICE_GATE_INCOMPLETE`，
   与 12:15 `.61` 9/9、12:18 `.53` cold_start PASS 冲突。
3. **进度台账缺当日条目**：`今日项目进度.txt` 无 2026-09-10 章节，12:15–12:33 的五机闭合与 API 探测未登记。
4. **审计文档落后**：`DOCS/工程审计_迁出任务审核_20260910.md`（02:53）仍写 `.53` 无 final verdict、`.61` TRANSPORT_BLOCKED。
5. **工作树不干净**：`git status` 有大量修改与未跟踪目录（`.codegraph/`、`.serena/`、`tests/touchsprite_migration/` 等），
   历史 `Z0-GIT` 绿项不得直接继承为本轮完成。
6. **`.166`/`.53` 表述纪律**：必须保留“原矩阵 8/9 + replacement PASS”，不得写成原始 SUMMARY `all_cases_pass=1`。

---

## 4. 建议的唯一一刀

先修**状态污染**，再谈新测试：

1. 修 `tools/test_checkpoint_finished_state.py`：改为临时路径 + 先备份/后还原真实 checkpoint；
   必要时给 `ziyan_codex_checkpoint.py` 加 `--state` 覆盖项。
2. 用真实断点重写 `.codex/ZIYAN_ACTIVE_CHECKPOINT.json`（`unfinished=true`，
   `nextAction` 指向第 2 节第 1 项或第 2 项），并重新 emit 续接提示词。
3. 本刀**不开新真机测试、不重跑 E48、不改 `lua/`/`objc/`/Agent**；设备只读核对当前包版本。

---

## 5. 边界

- 本轮未连接设备、未安装、未部署、未杀 SB/BB、未开 Agent/Smoke/Chat。
- 结论只来自磁盘证据与上述路径原文；未把 `.149/.171` 观察机当作 ZiYan PASS 来源。
- 未满足 ROADMAP §0 两条硬指标前，禁止任何“超越触动”表述。

---

## 6. 本轮执行结果（2026-09-10 17:20–21:50）

### 6.1 已完成

- **包统一**：`.53/.61` 由 `17-155` 降级到 `17-153`；五机 = rootful `17-152` / rootless `17-153`。证据 `tmp_shots/P2_UNIFY_PKG_20260910_172936/`。
- **`.61` 通道根治**：`mobile` 密码通道间歇性拒登（约 3 次 1 拒）；部署授权公钥后 `BatchMode` 10/10 OK。
- **受控重载 + framecap 重建**：五机 `sbreload` 后 `hooks sb_pid` 全部对齐；五机 framecap 加载当前包二进制。
- **门禁脚本修正 7 处**（`tools/zy_p2_c98_30m_gate.sh`）：var 路径按已装包 arch；每机生效 app/color/user 落盘；`last_color` 贪婪匹配修正；Home 带 `owner=com.ziyan.ziyan` 且先回 ZiYan App（2026-09-04 合同）；Home 原子写；Home 前停写 `open_app` 静置 3s；`fc_n()` 排除其它 grep；`ts_snap()` 补 `root`。
- **合同测试**：新增 `tools/test_p2_gate_var_path_contract.py`（10 项）全绿。
- **金标坐实**：`.53` app=`com.ljzbbadao.game`、金标 `16775151`，用独立 `/snapshot` 路径复算一致。
- **五机 Z2-30M**：`.112/.166/.53/.61` **30/30 PASS**；`.101` 第 12 分钟仅金标像素失败（详见 `今日项目进度.txt` 2026-09-10 章节）。
- **checkpoint 污染已修**：`ziyan_codex_checkpoint.py` 增加 `--state`、删除硬编码 worktree 回退；`test_checkpoint_finished_state.py` 只写临时文件并断言真实断点 sha256 不变；`test_checkpoint_contract.py` 改为断言结构而非历史 stage 字符串。实测三处 checkpoint 合同跑完，真实断点 sha256 前后一致。
- **断点与续接提示词已重写**：`.codex/ZIYAN_ACTIVE_CHECKPOINT.json` = `stage=Z2-30M-4-OF-5-PASS-101-COLOR-MISMATCH`、`unfinished=true`；`.codex/CONTINUATION_FIRST_MESSAGE.txt` 已替换过期 E48 正文。

### 6.2 未完成 / 阻塞

| 项 | 状态 | 证据 |
|---|---|---|
| `.101` Z2-30M | **未过**（第 12 分钟金标像素不匹配；SB/BB/FC 全程稳定） | `tmp_shots/P2_30M_C98_20260910_210210/101/SUMMARY.txt`、`tmp_shots/P2_101_RECOVER_20260910_213508/` |
| 五机 Z2-30M 全绿 | **未达成**（4/5） | 同上 `REPORT.md` |
| 五机 E48 在 152/153 上重跑 | 未做（E48 证据仍是 147/148/151） | `tmp_shots/E48_DEVICE*_2026090{9,10}_*` |
| `.61` 专用 rootless contract 工件 | 仍 `INVALID_RUN`（06:10） | `tmp_shots/E48_DEVICE61_ROOTLESS_CONTRACT_FINAL_20260910_061021/` |
| 迁移样本运行化 / 101 项功能矩阵 / 动态库加载 / 发布判定 | 未启动 | `tests/touchsprite_migration/`、`api_spec/device_function_matrix.json` |

### 6.3 本轮修掉的两个「假 FAIL」根因（都可复现）

1. `fc_n()` 把 zydaemon 看门狗自身的 `grep -F ziyan_framecap serve` 计成第 2 个宿主 —— `.53` 60 次采样误报 4 次；修后 60/60 全为 1。
2. Home 请求非原子写被 SB 读到截断空文件后静默丢弃 —— `.101` 第一轮第 3 分钟 `HOK=0 HOME_MS=8000`；同序列复测 10/10 正常。

### 6.4 不在本轮范围

未改 `lua/`、`objc/`、Agent、`Chat.lua`、桌面 `ios7.lua/ios8p.lua`；未杀 SB/BB；未把 `.149/.171` 当作 ZiYan PASS 来源；未宣布「超越触动」。
