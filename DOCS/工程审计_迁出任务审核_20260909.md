# 工程审计与迁出任务审核

日期：2026-09-09
工作目录：`/Users/mac/Desktop/ZiYan_副本`
依据：`/Users/mac/Desktop/工程审计总提示词.txt`、迁出任务完整回执、当前 checkpoint 和磁盘证据。

## 审核结论

当前任务没有完成主计划，完成度停留在“来源只读采集、迁移副本建立、E48 受控样本的部分五机验收”。主计划方向没有被产品代码改动带偏，但执行过程存在两类进度表达错误：

1. 把静态来源采集、API 矩阵生成和迁移映射误写成运行兼容性完成。
2. 把 `.166` 原矩阵的 `8/9` 与独立补测的 `1/1` 压成原矩阵 `9/9`；正确表述应为：原矩阵保留 `8/9`，唯一缺项通过独立 replacement PASS，当前九个选择用例均有独立 PASS 证据，但原始 SUMMARY 仍不是 `all_cases_pass=1`。

## 当前真实完成项

### Phase 0 / 0.4

- 已建立 `.48` Windows 来源的只读连接审计和来源清单。
- 已记录 `windows-ssh`、PowerShell encoded command、来源根 `E:\传奇世界`、文件数量和敏感排除数量。
- `e48_legend_manifest.json`：允许记录 40 个，敏感排除 41 个。
- 当前工作树不是干净状态，不能把历史 `Z0-GIT` 绿项直接继承为本轮完成。

### Phase 0.5

- 22 个迁移候选已经复制到 `tests/touchsprite_migration/source/`，`sample_mapping.json` 显示 22 个 `copyStatus=verified`。
- 这 22 个文件仍是来源副本和静态映射，不是 ZiYan 可运行样本。
- `api_mapping.json` 明确 `staticOnly=true`；映射状态为 `partial_mapping_requires_rewrite` 9 个、`dependency_only_or_no_allowlisted_api` 8 个、`unmigratable_static_only` 3 个、`direct_api_mapping` 1 个、`resource_only` 1 个。
- 语法检查有 12 个失败、9 个通过、1 个不适用；失败项不能自动提升为兼容通过。
- `ziyan_business_sample.lua` 已存在并被 E48 runner 使用，但它是 ZiYan 自有非 Agent 受控样本，不等于 22 个来源脚本已迁移运行。

### E48 当前包受控验收

- `.101`：`tmp_shots/E48_DEVICE101_20260909_211121/SUMMARY.txt`，9/9；当前 rootful 包 SHA 为 `cba354ba25511226431b78c64d57c9c9c04a05d777e5e7ed0427cc624a91826a`；各用例有独立 `run_id`、当前脚本/资源 SHA、设备端 `final_verdict=PASS` 和 `final_cleanup=1`。
- `.112`：`tmp_shots/E48_DEVICE112_20260909_204440/SUMMARY.txt`，9/9；同一当前 rootful 包和独立设备证据。
- `.166`：原矩阵 `tmp_shots/E48_DEVICE166_20260909_213954/SUMMARY.txt` 为 8/9，`cold_start` 因 `foreground_active_seen=0` 为 `INVALID_RUN`；随后仅重做该缺项，`tmp_shots/E48_DEVICE166_20260909_214618/` 的 `run_id=e48_cold_start_166_18548_1788961579` 为 `final_verdict=PASS`、`final_cleanup=1`。不得重跑其余 8 项。
- `.53`：当前 E48 业务 final verdict 尚未建立；rootless 包 SHA 为 `a82f2a5454b3b17721ee43ddd9c56a52bd1ea6f8258811c52bc4dc80946d8ab8`。
- `.61`：iPhone 7、iOS 15.8.8、rootless；当前只有早期 SSH 注册线索，尚无本轮 rootless contract final verdict。

## 反向审核：偏离、重复和缺口

### 已按方向完成

- `.101 -> .112 -> .166` 顺序正确。
- `.112` 的 runner 证据注入问题已被修复并获得当前包 9/9 证据。
- `.166` 按唯一缺项做了独立补测，符合“不重复已通过内容”的原则。
- 未把 `.149/.171` 当成 ZiYan PASS 来源。

### 需要纠正

- 旧续接正文仍描述 `.112` 多轮 `INVALID_RUN`，与当前 `.112` 9/9 证据冲突。
- `connection_inventory.md` 仍保留“迁移仅 dry-run、没有本地落盘”的旧叙述，与当前 22 个 verified source files 和 `sample_mapping.json` 冲突；应把它视为过期报告，后续任务需要补一份当前状态汇总，不得用旧句子判定未复制。
- `api_101_static_audit.json` 的 101 项全部为设备 `NOT_RUN`，所以它证明的是能力矩阵尚未完成，不是 E48 九用例失败。
- 动态库报告仍是 `STATIC_REVIEW_ONLY`；rootless `/var/jb` 路径、Lua 核心库和 readline 闭包没有设备加载证据。
- 不能因为 ROADMAP 中历史 Z1/Z2 项为绿，就跳过当前包的非 Agent API、迁移样本、动态库和五机发布门槛。

## 主计划剩余顺序

1. `.53`：安装并核对 rootless 包 `...148`，执行现有 E48 matrix，收集每项完整设备证据。
2. `.61`：恢复授权 mobile SSH key；认证失败记录 `TRANSPORT_BLOCKED`，认证恢复后执行 iOS 15.8.8 rootless contract。
3. 只有 `.101/.112/.166/.53/.61` 均有当前包、当前脚本、当前资源、唯一 run_id、设备端 final verdict、日志/结构化结果、截图或等价证据和 cleanup evidence，才能关闭五机 E48 门禁。
4. 进行 Phase 0.5 迁移样本的逐样本可执行重写和最小复现；保留来源 SHA、目标 SHA、API 映射、缺口、回滚方式和五机 run_id。
5. 推进 101 项非 Agent API/能力矩阵；`local_functional_pass=false` 和设备 `NOT_RUN` 必须保持，按能力族分批验证，不重复 E48 已覆盖的生命周期样本。
6. 完成动态库清单、路径/依赖修复候选、rootful/rootless 加载验证和五机回归。
7. 回归核心生命周期、触控、图色、OCR、Lua、IPC、文件网络、脚本管理、异常退出、停止、重复运行、资源缺失和网络异常。
8. 建立缺陷清单、修复提交、五机状态和正式版发布判定；在 `unfinished=false` 且 `nextAction=等待人工最终审核` 前不得宣称主计划完成或“超越 TouchSprite”。

## 当前唯一下一刀

只处理 `.53` rootless E48；不重跑 `.101/.112/.166`，不修改 `lua/`、`objc/`、Agent、`Chat.lua` 或桌面 `ios7.lua/ios8p.lua`。完成后更新 checkpoint，并在同一任务末尾主动汇报完成项、未完成项、阻塞项、checkpoint 和剩余主计划。

