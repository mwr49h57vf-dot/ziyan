# 工程审计与迁出任务审核

日期：2026-09-10（证据时间）

> **2026-09-10 21:00 之后的更正（由本文件下方结论推翻的部分）**
> 本文件 02:53 版的两处表述已过期：
> 1. 「`.53` 当前 E48 业务 final verdict 尚未建立」——12:18 已建立：`tmp_shots/E48_DEVICE53_20260910_121759/`，
>    `run_id=e48_cold_start_53_21308_1789013880`、`foreground_active_seen=1`、`final_verdict=PASS`、`final_cleanup=1`。
> 2. 「`.61` 尚无本轮 rootless contract final verdict」——12:15 已建立九用例矩阵
>    `tmp_shots/E48_DEVICE61_20260910_121258/SUMMARY.txt`（`total_cases=9 pass_cases=9 all_cases_pass=1`），
>    包 `…17-151+debug`（SHA `5f62c6ce…1168`）；06:10 的专用 rootless contract 工件仍为 `INVALID_RUN`。
> 3. 12:15–13:07 又发生两批未登记事实：五机 API 绑定探测
>    `tmp_shots/API101_DEVICE*_20260910_1233/`（`total=101 present=86 missing=15`），
>    以及 5 次五机 Z2-30M 失败（`tmp_shots/P2_30M_C98_20260910_124944` … `_130721`，均 minute 1 FAIL）。
> 4. 五机 Z2-30M 已于当日 21:02–21:32 重跑：`.112/.166/.53/.61` 30/30 PASS，
>    `.101` 第 12 分钟因金标像素不匹配 FAIL。详见 `今日项目进度.txt` 2026-09-10 章节。

工作目录：`/Users/mac/Desktop/ZiYan_副本`
审核对象：Codex 任务 `一进度`（`01a086f2-9b1a-7481-afe6-b3661d906c8f`）
依据：
- `/Users/mac/Desktop/工程审计总提示词.txt`
- `DOCS/工程审计_迁出任务审核_20260909.md`
- `.codex/ZIYAN_ACTIVE_CHECKPOINT.json`
- `tmp_shots/E48_DEVICE53_20260910_002642/`
- `tmp_shots/E48_DEVICE61_AUTH_20260910_003233/`
- `tests/touchsprite_migration/`

## 审核结论

本次任务没有完成主计划，也没有改变主计划方向。它正确推进了 `.53` rootless E48，并在 `.61` 通道失败后停止产品测试；当前主计划仍停在五机 E48 门禁之前。

实际完成度应表述为：

- Phase 0/0.4：Windows 来源只读审计和快照已完成。
- Phase 0.5：22 个来源副本已验证复制；迁移运行仍未完成。
- E48：`.101`、`.112` 当前包 9/9；`.166` 原矩阵 8/9 加唯一 `cold_start` replacement PASS；`.53` 当前包 8/9 PASS 加 1 个 `cold_start INVALID_RUN`；`.61` 仅完成公钥认证探测并记录 `TRANSPORT_BLOCKED`。
- 五机 E48：未完成。
- 非 Agent 101 项能力矩阵：未完成，仍为静态/未运行证据。
- 动态库运行加载闭环：未完成，仍为静态审查。
- 发布和“超越 TouchSprite”判定：未满足。

## 反向审核结果

### 1. 是否偏离主计划

未发现产品代码方向偏离。任务遵守了当前唯一下一刀：

- 先处理 `.53`；
- 没有重跑 `.101/.112/.166` 已通过用例；
- `.53` 使用 rootless 包和 `/var/jb` 路径；
- `.61` 只做授权公钥探测，认证失败后没有安装包、启动脚本或修改设备；
- 未把 `.149/.171` 当成 ZiYan PASS。

### 2. 是否重复已测试内容

没有发现违反“不得重复已通过内容”的设备级重复：

- `.166` 只补做原矩阵唯一缺项 `cold_start`，没有重跑其余 8 项。
- `.53` 的 9 项是该设备当前 rootless 包的首次矩阵；`cold_start` 为通道/场景证据不足的 `INVALID_RUN`，不能当作产品失败，也不能当作 PASS。
- `.61` 认证失败后没有重复安装或业务执行。

需要保留的边界：`.53` 的 `repeated_run_1`、`repeated_run_2`、`rerun_after_stop` 属于该 E48 矩阵明确定义的生命周期用例，不是无计划重复已完成设备的测试。

### 3. 是否按要求完成每一步

已按要求完成：

- `.53` 包版本、包 SHA、基础脚本 SHA、资源 SHA 已记录。
- 8 个 `.53` PASS 用例均有独立 `run_id`、设备端 `final_verdict=PASS`、日志/结构化结果和 `final_cleanup=1`。
- `.53` `cold_start` 保留了完整 `INVALID_RUN` 原因：`foreground_active_seen=0`，同时清理完成。
- `.61` 的 `Permission denied (publickey,password,keyboard-interactive)` 已原样落盘，裁决为 `TRANSPORT_BLOCKED`。
- checkpoint 已保持 `unfinished=true`，没有提前关闭主计划。

未按最终门槛完成：

- `.53` `SUMMARY.txt` 明确为 `total_cases=9`、`pass_cases=8`、`all_cases_pass=0`，因此不能写成 `.53` 完整 E48 PASS。
- `.61` 没有当前包安装、rootless contract、脚本/资源导入、业务 final verdict 或 cleanup 证据。
- 五机 E48 门禁尚未更新。
- 22 个迁移文件仍是来源副本；`api_mapping.json` 仍为 `staticOnly=true`。
- 101 项 API 矩阵仍为 `localFunctionalPass=false`，设备行仍为 `NOT_RUN`。
- 动态库仍只有 `STATIC_REVIEW_ONLY`，没有设备加载证据。

## 文档和口径问题

1. `DOCS/工程审计_迁出任务审核_20260909.md:36` 已落后于实际证据，仍写 `.53` 尚无 final verdict；应以本报告和当前 checkpoint 为准。
2. 旧续接正文仍有 `.112` 多轮 `INVALID_RUN` 描述，但当前 `.112` 证据为当前包 9/9；旧描述不能用于当前完成度。
3. `connection_inventory.md` 的“没有本地落盘”叙述与 22 个 `copyStatus=verified` 文件冲突，应标注为历史状态，不能据此否认当前复制结果。
4. `.166` 必须保持“原矩阵 8/9 + cold_start replacement 1/1”，不能压成原始 `SUMMARY all_cases_pass=1`。

## 当前证据表

| 设备 | 当前结果 | 可采信范围 | 缺口 |
|---|---|---|---|
| `.101` | E48 9/9 PASS | 当前 rootful 包和逐项设备证据 | 后续非 Agent API、动态库、核心回归 |
| `.112` | E48 9/9 PASS | 当前 rootful 包和逐项设备证据 | 后续非 Agent API、动态库、核心回归 |
| `.166` | 原矩阵 8/9；cold_start replacement PASS | 九个选择用例均有独立证据 | 原始矩阵仍非 `all_cases_pass=1` |
| `.53` | 8/9 PASS；cold_start INVALID_RUN | 当前 rootless 包和 8 个独立 PASS | cold_start 需在有效前台条件下补测 |
| `.61` | TRANSPORT_BLOCKED | 公钥认证失败证据 | authorized mobile SSH key、rootless contract、完整 final verdict |

`.53` 当前包 SHA：`a82f2a5454b3b17721ee43ddd9c56a52bd1ea6f8258811c52bc4dc80946d8ab8`

## 主计划剩余顺序

1. 人工恢复 `.61` 的 authorized `mobile` SSH key；认证未恢复时只记录 `TRANSPORT_BLOCKED`，不重复测试其他设备。
2. `.61` 认证恢复后，仅执行 iOS 15.8.8 rootless contract，采集当前包/脚本/资源 SHA、唯一 `run_id`、设备端 `final_verdict`、日志或结构化结果、截图或等价证据和 cleanup evidence。
3. `.61` 证据完整后，重新计算五机 E48 门禁；不得把 `.53` 的 `8/9 + INVALID_RUN` 写成完整 PASS。
4. 五机 E48 门禁后，逐样本进行 TouchSprite 迁移可执行重写和最小复现；保留来源 SHA、目标 SHA、API 映射、缺口、回滚和五机 run_id。
5. 按能力族运行 101 项非 Agent API 矩阵，不重复 E48 已覆盖的生命周期样本；保持静态未运行项为 `NOT_RUN`，直到真实设备证据产生。
6. 完成动态库清单、rootful/rootless 路径和依赖修复候选、设备加载验证及五机回归。
7. 回归核心生命周期、触控、图色、OCR、Lua、IPC、文件网络、脚本管理、异常退出、停止、重复运行、资源缺失和网络异常。
8. 建立缺陷清单、修复提交、五机状态和正式版发布判定；在 `unfinished=false` 且 `nextAction=等待人工最终审核` 前，不得宣称主计划完成或“超越 TouchSprite”。

## 本轮交接唯一动作

恢复 `.61` authorized mobile SSH key 后，只执行 `.61` iOS 15.8.8 rootless contract。不得重跑 `.101/.112/.166/.53`，不得修改 `lua/`、`objc/`、Agent、`Chat.lua` 或桌面 `ios7.lua/ios8p.lua`。完成或阻塞后必须主动回报完成项、未完成项、阻塞原因、checkpoint 和剩余主计划。
