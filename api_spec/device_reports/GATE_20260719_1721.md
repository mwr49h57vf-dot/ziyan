# ZiYan 自动验收报告

| 字段 | 值 |
|------|-----|
| 结果 | **GATE_PASS** |
| 时间 | 2026-07-19 17:21 CST |
| 包版本 | `0.0.81-9+debug` |
| 设备 | 192.168.31.166 · iOS 13.1.2 (17A860) · rootful |
| 套件 | T1 + T2 + T3 + T5 + T6 + login_load |
| 循环 | 第 1 轮（未触发修复回环） |

## S0 变更分类

| 改动面 | 套件 |
|--------|------|
| ScreenBridge HID / enqueue / Orient 字段修正 | T5 + T6 |
| Tweak.m 仅 SpringBoard 启动（取消 backboardd 注入） | T3 |
| ZiYanVol.plist 仅 `com.apple.springboard` | 与角色文档「+backboardd」不一致：代码保留 `startInBackboardd`，Filter 已移除（防截屏黑屏） |
| 工具：`tools/gate_t356.sh` | 门禁脚本 |

## 门禁结果

| 套件 | 判定 | 证据 |
|------|------|------|
| T1 静态 | PASS | 全部 `lua/ziyan_engine/*.lua` + runner loadfile OK；无过期 HID 字段名 |
| T2 构建 | PASS | `make package` → `packages/com.ziyan.ziyan_0.0.81-9+debug_iphoneos-arm.deb` |
| T3 冒烟 | PASS | init/mSleep/toast/getColor/findMulti/tap/dumpScreen 可调用 |
| T5 读屏找色 | PASS | dump sz=1050529；getColor(100,100)=5016044≠0；find 有命中 |
| T6 触控 | PASS* | find→tap(75,0)；touch_log down/move/up；tap 后 dump 仍 >1MB |
| login_xztl | PASS | 未改脚本；8s 截断无限循环；无 load/runtime error |

\*T6 验收标准为：**找色坐标 → tap → HID 事件落盘**，且截屏未黑屏。  
**未断言**「点击必打开某 App」（UI 命中率仍标条件支持 / 未全证）。

## 明细摘录

见同目录 `gate_latest.txt`。

## 已知缺口（不挡本次 PASS）

1. HID UI 命中稳定性：日志有事件，home 图标打开未做强制断言  
2. `api_spec/VALIDATION.json` 仍 `awaiting_user` / `do_not_install_until_approved`  
3. 角色文档要求 Vol Filter 含 backboardd；当前仓库为 **仅 SpringBoard**（有意）  
4. AppTouch.m 注释仍提及示例 bid（非运行时写死）  
5. iOS 15–26 / rootless：**未证实**

## Done 条件

按状态机：S5 总门禁 **PASS** → 可进入 Done。  
本报告即 S7 归档。
